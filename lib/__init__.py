"""any2video — deep-video skill toolbox.

Each submodule exposes a CLI (`python -m lib.X.Y --help`) returning JSON to
stdout. An agent orchestrates by reading the JSON and deciding next steps.
The skill prompt itself lives at skill/SKILL.md.
"""
__version__ = "0.1.0"
